# Smart Shopping Cart

* Clone the repository
* Run `npm install` to install dependencies
* Run `npm run test` to execute Specifications and see the result

# Configure Different discounts

* Update `Configurator` class to add more clients with special deals

# Add additional products

* Update `Configurator` class to add more Products to `ProductCatalog`
